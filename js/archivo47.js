let x = 10, y = 25, z = 10;  

if (x === y && y === z) {
    console.log("Los tres números son iguales.");
} else {
    console.log("El mayor es:", Math.max(x, y, z));
    console.log("El menor es:", Math.min(x, y, z));
}
