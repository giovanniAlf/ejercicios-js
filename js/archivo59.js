let edad = 18, estatura = 180, peso = 78;  // Ejemplo

if (edad <= 19 && estatura > 175 && peso >= 75 && peso <= 80) {
    console.log("Cumple requisitos para la selección.");
} else {
    console.log("No cumple los requisitos.");
}
